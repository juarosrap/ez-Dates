/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const prefedadAPI_auto = {

    /** Gets all entries from 'prefedad' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/prefedad`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'prefedad' by its primary key */
    getById: async function(prefEdadId) {
        let response = await axios.get(`${BASE_URL}/prefedad/${prefEdadId}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'prefedad' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/prefedad`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'prefedad' by its primary key */
    update: async function(formData, prefEdadId) {
        let response = await axios.put(`${BASE_URL}/prefedad/${prefEdadId}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'prefedad' by its primary key */
    delete: async function(prefEdadId) {
        let response = await axios.delete(`${BASE_URL}/prefedad/${prefEdadId}`, requestOptions);
        return response.data;
    },
};

export { prefedadAPI_auto };
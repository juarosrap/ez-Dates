/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const prefpeloAPI_auto = {

    /** Gets all entries from 'prefpelo' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/prefpelo`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'prefpelo' by its primary key */
    getById: async function(prefPeloId) {
        let response = await axios.get(`${BASE_URL}/prefpelo/${prefPeloId}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'prefpelo' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/prefpelo`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'prefpelo' by its primary key */
    update: async function(formData, prefPeloId) {
        let response = await axios.put(`${BASE_URL}/prefpelo/${prefPeloId}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'prefpelo' by its primary key */
    delete: async function(prefPeloId) {
        let response = await axios.delete(`${BASE_URL}/prefpelo/${prefPeloId}`, requestOptions);
        return response.data;
    },
};

export { prefpeloAPI_auto };